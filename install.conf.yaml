- defaults:
    link:
      relink: true

# - clean: ['~']

- link:
    ~/.tmux.conf: tmux.conf
    ~/.vim: vim
    ~/.vimrc: vimrc

- create:
    - ~/Downloads
    - ~/.vim/undo-history
    - ~/Work
    - ~/Sites
    - ~/Research

# - shell:
#  - [git submodule update --init --recursive, Installing submodules]
